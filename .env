# Database Configuration
- AZURE_SQL_SERVER=ati-portal.database.windows.net
- AZURE_SQL_PORT=1433
- AZURE_SQL_USERNAME=atiportal-dbadmin
- AZURE_SQL_PASSWORD=infy@123
- AZURE_SQL_DATABASE=ati-portal

# Azure AD Configuration
AZURE_AD_TENANT_ID=your_tenant_id
AZURE_AD_CLIENT_ID=your_client_id
AZURE_AD_CLIENT_SECRET=your_client_secret

# JWT Configuration
JWT_SECRET=your_jwt_secret
JWT_EXPIRATION=1d

# OneDrive Configuration
ONEDRIVE_HR_FOLDER_PATH=/HR Documents
# We'll use the same Azure AD credentials for OneDrive access
# since it's part of the Microsoft 365 suite
# AZURE_AD_TENANT_ID is already configured above
# AZURE_AD_CLIENT_ID is already configured above
# AZURE_AD_CLIENT_SECRET is already configured above